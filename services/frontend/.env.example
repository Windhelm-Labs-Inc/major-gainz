# Frontend Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# ==========================================
# PUBLIC: Build-time Variables
# ==========================================
# These are safe to expose in the frontend bundle

# WalletConnect Project ID (public, safe to expose)
VITE_WALLETCONNECT_PROJECT_ID=0e73bf40d90f03a83b1fed4375e5fe04

# Hedera Network Selection (mainnet is default for production)
VITE_HEDERA_NETWORK=mainnet

# For development/testing, you may want to use testnet:
# VITE_HEDERA_NETWORK=testnet


# ==========================================
# BACKEND PROXY ENDPOINTS
# ==========================================
# The frontend calls these backend endpoints instead of external APIs directly:
# - /chat/completion (OpenAI proxy)
# - /portfolio/* (Portfolio data)
# - /defi/* (DeFi data)
# - /tokens/* (Token data)
# - /ohlcv/* (Price data)
